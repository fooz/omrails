<% if user_signed_in? %>
	<h1>You're logged in</h1>
<% else %>
<div class="hero-unit">
	<h1>Welcome to OM Rails</h1>
	<p>
		This is the homepage for the
		<%= link_to "OM Rails", "#" %> thing.
	</p>
	<p><%= link_to "Sign Up Now!", new_user_registration_path, class: "btn btn-primary btn-large" %>
</div>
<% end %>